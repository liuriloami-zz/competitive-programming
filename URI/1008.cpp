#include <bits/stdc++.h>
using namespace std;
 
int main (int argc, char** argv) {
    int n;
    double h, v;
    cin >> n >> h >> v;
    printf("NUMBER = %d\n", n);
    printf("SALARY = U$ %.2f\n", h*v);
    return 0;
}
